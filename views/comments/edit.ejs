<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Comment</title>
</head>
<body>
    <% if (comment) { %>
        <h1>Edit Comment</h1>
        <form action="/comments/<%= comment.id %>/?_method=PATCH" method="post">
            <textarea name="comment" cols="30" rows="10"><%= comment.comment %></textarea>
            <br>
            <button>Submit Edit Save</button>
        </form>
    <% } else { %>
        <h1>Comment not found</h1>
    <% } %>
</body>
</html>
